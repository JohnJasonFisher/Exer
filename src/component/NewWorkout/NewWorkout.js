import React from 'react'

const NewWorkout = props => {
	return (
		<div className='NewWorkout'>
			<button>Start a new Workout!</button>
			<button>Finish a new Workout!</button>
		</div>
	)
}

export default NewWorkout